#!/bin/bash
set -e

source ./_common.sh

init_chroot

/usr/bin/mock -r ${CHROOT} --shell "

mkdir -p ~/.quake2world/default/maps
touch ~/.quake2world/default/maps/torn.bsp
echo -e '// generated by Quake2World, do not modify\n' > ~/.quake2world/default/quake2world.cfg

cd /tmp/quake2world
autoreconf -i --force
./configure --prefix=/tmp/quake2world-'${CHROOT}' --with-tests --with-master --without-client
make
make check
make install
" 

archive_workspace
destroy_chroot
